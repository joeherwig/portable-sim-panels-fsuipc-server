<Window x:Class="portableSimPanelsFsuipcServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:portableSimPanelsFsuipcServer"
        mc:Ignorable="d"
        Title="portable-sim-panels-FSUIPC-server" Height="400" Width="600" Background="{DynamicResource BackgroundColor}" WindowStyle="None"
        MouseDown="Window_MouseDown" MinWidth="400" MinHeight="250">
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="#000000"/>
        <SolidColorBrush x:Key="TextColor" Color="#F2FFFFFF"/>
        <Style TargetType="{x:Type Window}">
            <Setter Property="Background" Value="{StaticResource BackgroundColor}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"></Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="0,0,-5,5" MinWidth="300" MinHeight="300" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="200"/>
        </Grid.ColumnDefinitions>

        <DockPanel LastChildFill="False" Margin="10,10,0,11" RenderTransformOrigin="-0.723,-0.896" HorizontalAlignment="Left" Width="75">
            <DockPanel.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-0.008"/>
                    <TranslateTransform/>
                </TransformGroup>
            </DockPanel.RenderTransform>
            <Button Content="Button" Width="75" Height="75" VerticalAlignment="Top"
                x:Name="buttonFS" ContentTemplate="{StaticResource iconFS}" BorderBrush="#00000000" Cursor="Arrow"/>
        </DockPanel>
        <Grid Margin="100,0,10,0" Height="32" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Center">

                <Button x:Name="MinimizeButton"
                KeyboardNavigation.IsTabStop="False"
                Click="MinimizeWindow" ContentTemplate="{StaticResource iconMinimize}" Width="32" BorderBrush="{x:Null}" />

                <Button x:Name="MaximizeButton"
                KeyboardNavigation.IsTabStop="False"
                Click="MaximizeWindow" ContentTemplate="{StaticResource iconMaximize}" BorderBrush="{x:Null}" />

                <Button x:Name="CloseButton"
                KeyboardNavigation.IsTabStop="False"
                Click="ButtonClose_Click"  ContentTemplate="{StaticResource iconClose}" BorderBrush="{x:Null}"/>
            </StackPanel>

        </Grid>
        <Grid HorizontalAlignment="Left" Height="100" Margin="337,185,0,0" VerticalAlignment="Top" Width="100"/>
        <Grid Margin="100,37,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="246*"/>
                <ColumnDefinition Width="204*"/>
                <ColumnDefinition Width="23*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="JsonFilterField" Height="23" TextWrapping="Wrap" Text="filter" Background="{DynamicResource BackgroundColor}" Foreground="{DynamicResource TextColor}" Margin="10,0" VerticalAlignment="Top" Grid.ColumnSpan="4"/>
            <TextBox x:Name="JsonTextField" Margin="10,28,10,10" TextWrapping="Wrap" Grid.ColumnSpan="3" Foreground="{DynamicResource TextColor}" AcceptsReturn="True" BorderBrush="{x:Null}" Background="{DynamicResource BackgroundColor}" OpacityMask="#7F2A2A2A"/>
        </Grid>
        <Image Source="airplane-airline-aircraft-travel.jpg" Panel.ZIndex="-1" Stretch="UniformToFill"/>
    </Grid>
</Window>
